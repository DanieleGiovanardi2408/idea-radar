# Build per l'app desktop (vite --mode desktop): la webview Tauri non ha il
# proxy Vite, quindi l'API va raggiunta direttamente sulla porta del sidecar.
# La porta è fissata in src-tauri/src/main.rs (BACKEND_PORT): cambiala in
# entrambi i posti o in nessuno.
VITE_API_BASE=http://127.0.0.1:8765
